// 模块五：Transformer 与 GPT（6 课）
const C = { blue: '#2563eb', red: '#dc2626', green: '#059669', amber: '#d97706', violet: '#7c3aed', pink: '#db2777' }

export const m5 = {
  id: 'm5',
  num: 5,
  emoji: '⚡',
  title: 'Transformer 与 GPT',
  color: '#db2777',
  subtitle: '大模型的心脏：注意力、架构、自回归生成',
  lessons: [
    // ============ 5.1 ============
    {
      id: 'm5l1', num: '5.1', title: '注意力机制：让每个词环顾四周', minutes: 20, demoCount: 1,
      emoji: '👀',
      brief: 'Transformer 的灵魂操作：每个词按相关度加权吸收全句信息。',
      blocks: [
        { t: 'callout', kind: 'story', title: '一句话论文', md: '2017 年《Attention Is All You Need》提出的 **Transformer** 架构完全建立在注意力之上。十年后的今天，GPT、Claude、Gemini、LLaMA、DeepSeek……你听过名字的大模型，心脏全都是它。本模块把它彻底拆开。' },
        { t: 'h', text: '自注意力：一句话里的「圆桌会议」' },
        { t: 'p', md: '**自注意力（self-attention）**就是让序列里的每个词，都对全句做一次「环顾」，分三步走：**① 打分**——和每个词算一个相关度分数；**② 变权重**——把分数过 softmax 变成总和为 1 的注意力分配；**③ 加权吸收**——按权重把所有词的信息加权汇总。于是「它」这个词的输出向量里，自然就包含了它指向的「小猫」的信息——**指代消解一步到位，不需要任何循环和排队**。而「自」的含义是：参与会议的就是本句话自己（自己打自己的分），没有外部信息源。' },
        { t: 'formula', tex: `\\textcolor{${C.blue}}{\\text{Attention}(Q,K,V)} = \\textcolor{${C.violet}}{\\text{softmax}}\\!\\left( \\frac{\\textcolor{${C.red}}{Q K^{\\top}}}{\\textcolor{${C.amber}}{\\sqrt{d}}} \\right) \\textcolor{${C.green}}{V}`,
          terms: [
            { color: C.blue, frag: '\\text{Attention}', name: '注意力输出', desc: '每个词「吸收完上下文」后的新表示' },
            { color: C.red, frag: 'QK^{\\top}', name: '打分矩阵', desc: '每个词的查询与所有词的键做点积 = 相关度' },
            { color: C.amber, frag: '\\sqrt{d}', name: '缩放因子', desc: '按向量维度缩放，防止 softmax 饱和' },
            { color: C.violet, frag: '\\text{softmax}', name: '变权重', desc: '把分数变成总和为 1 的注意力分配' },
            { color: C.green, frag: 'V', name: '值矩阵', desc: '每个词实际携带的信息，按权重被吸收' },
          ],
          note: '这个公式就是 Transformer 的全部核心。别怕，两节课内你会亲手把它算两遍。' },
        { t: 'h', text: '先用 2 个词把公式算一遍' },
        { t: 'example', title: '算一遍：最小注意力（只有 2 个词）', steps: [
          '句子「小猫 它」。为能心算，用 1 维的 Q/K/V（真实模型是 64～128 维）。当前词「它」的查询 q = 2；两个词的键：小猫 k = 1，它自己 k = 0；两个词的值：小猫 v = 5（代表「小猫」的信息），它自己 v = 1。',
          '**① 打分**：它→小猫 = q×k = 2×1 = **2**；它→自己 = 2×0 = **0**。',
          '**② softmax 变权重**：e² ≈ 7.39，e⁰ = 1。权重 = 7.39/(7.39+1) ≈ **0.88** 和 **0.12**（总和 1）。「它」把 88% 的注意力给了小猫——模型学会了「它」在找指代对象。',
          '**③ 加权吸收**：输出 = 0.88×5 + 0.12×1 = **4.5**。',
          '对比一下：如果「它」和任何词都不相关（分数全为 0），权重各 0.5，输出 = 0.5×5+0.5×1 = 3。**注意力分数直接控制「谁的信息进入我的新表示、占多少比例」**。',
        ], result: '打分 → softmax → 加权求和，三步就是注意力的全部。**GPT 处理 1000 个词的文本，就是把这个三步流程跑 1000 次（每个词各当一次「它」）**——而且这 1000 次可以拼成两个大矩阵乘法一次算完（全并行）。' },
        { t: 'demo', id: 'attention', title: '注意力热力图实验室（点词 + 温度 + 因果掩码）' },
        { t: 'callout', kind: 'qa', title: '自问自答', md: '**「缩放 ÷√d 是在防什么？」**——点积的大小随维度增长：d=128 时，两个随机向量的点积轻松到几十，softmax 一算就「一家独大」（比如 0.999 和 0.001），梯度几乎为零，模型学不动。除以 √d 把分数拉回温和区间，softmax 保持「软」的状态。**「注意力权重是学出来的吗？」**——是！打分用的 Q/K 来自可学习矩阵（下一课），训练之前注意力是均匀的随机乱看，训练之后才长出「盯指代、盯搭配」的模式。演示里拖动温度滑块，你能看到 softmax 的「软硬」如何变化。' },
        { t: 'callout', kind: 'analogy', title: '打个比方', md: '注意力像**圆桌会议**：每个人（词）发言时，可以按「相关性」决定听谁多一点、听谁少一点，且所有人**同时**进行（并行）。RNN 是排队传话，注意力是圆桌直播——这就是 4.4 课说的三张新牌落到实处的样子。' },
        { t: 'h', text: '多头注意力：几双眼睛各看各的' },
        { t: 'p', md: '只做一次注意力，所有词只能学出「一种关注方式」，而这句子里同时存在多种值得注意的关系：语法搭配（形容词配名词）、指代（它→小猫）、长距离语义（开头的主语管到句尾的动词）。**多头注意力（multi-head attention）**并行跑多组独立的 Q/K/V——比如 GPT-3 把 12288 维向量切成 96 个头、每头 128 维：每个头在自己的小空间里独立做一遍完整注意力（维度小所以总计算量不变），有的头专门盯语法搭配、有的盯指代关系、有的盯长距离语义。各组输出拼接后再线性变换融合。就像请 96 个不同专长的专家同时读一句话，最后汇总意见——而且他们开会**互不干扰、同时进行**。研究者解剖训练好的模型时，确实找到了「指代头」「语法头」等分工明确的头，这个设计不是玄学。' },
        { t: 'quiz', questions: [
          { q: '自注意力中 softmax 的作用是？', opts: ['增加非线性', '把相关度分数变成总和为 1 的注意力权重（可微的「软选择」）', '减少参数量', '防止过拟合'], a: 1, why: 'softmax 让每个词把「注意力预算」按比例分配给全句各词，且处处可导、可以反向传播学习。' },
          { q: '为什么打分后要除以 √d（向量维度的平方根）？', opts: ['让结果为整数', '维度大时点积数值会变大，缩放防止 softmax 进入饱和区（梯度消失）', '统一单位', '没有实际作用'], a: 1, why: '点积的方差随维度增长；不缩放的话 softmax 一家独大、梯度趋零，训练不稳。' },
          { q: '多头注意力中「多头」的意义是？', opts: ['模型更吓人', '多组注意力并行学习不同的关系模式（语法/指代/语义…），最后汇总', '减少计算量', '提高数值精度'], a: 1, why: '每个头有独立 Q/K/V 权重，各自捕捉一种关系模式；拼接汇总让表示更丰富。' },
        ] },
        { t: 'summary', items: [
          '自注意力：**打分（Q·K）→ softmax 变权重 → 加权吸收 V**（2 词迷你算例你已手算）',
          '缩放 ÷√d 防 softmax 饱和；多头（GPT-3 有 96 个头）让模型同时学多种关系',
          '任意两词一步直连 + 全序列并行 —— RNN 的两大痛点同时解决',
          'Attention(Q,K,V) = softmax(QKᵀ/√d)·V 值得背下来',
        ] },
      ],
    },
    // ============ 5.2 ============
    {
      id: 'm5l2', num: '5.2', title: 'Q / K / V：图书馆检索的数学', minutes: 20, demoCount: 1,
      emoji: '🔍',
      brief: '查询、键、值到底怎么来的？为什么必须分成三份？手算一遍全明白。',
      blocks: [
        { t: 'h', text: '为什么一个词要有三个「分身」' },
        { t: 'callout', kind: 'analogy', title: '图书馆检索', md: '你想找书：心里带着**问题**（Query「我要讲注意力的书」）；每本书贴着**标签**（Key「书名/主题」）；书里的**内容**是 Value。检索 = 拿你的问题和每本书的标签比对，越匹配越多读它的内容。**注意：问题、标签、内容是三种不同的东西**——一本书的标签可能是「量子物理」，但内容讲的是量子计算史；你想找的可能是「通俗入门」。所以词向量要分别乘三个矩阵变成 Q、K、V——三个「分身」各司其职，这正是注意力能学出灵活关系的关键。' },
        { t: 'formula', tex: `\\textcolor{${C.blue}}{Q} = X W_Q \\qquad \\textcolor{${C.green}}{K} = X W_K \\qquad \\textcolor{${C.amber}}{V} = X W_V`,
          terms: [
            { color: C.blue, frag: 'Q', name: '查询 Query', desc: '「我在找什么信息」——由可学习矩阵 W_Q 变换而来' },
            { color: C.green, frag: 'K', name: '键 Key', desc: '「我能提供什么标签」——用于和别人的 Q 比对' },
            { color: C.amber, frag: 'V', name: '值 Value', desc: '「我实际携带的内容」——被加权汇总的信息本体' },
            { color: C.violet, frag: 'X', name: '输入向量', desc: '上一层输出的词表示' },
          ],
          note: 'W_Q、W_K、W_V 都是可学习参数——训练的任务之一就是学会「该怎么提问、贴什么标签、装什么内容」。初始时它们是随机的，注意力乱看一气；训练结束后才长出精准的目光。' },
        { t: 'p', md: '为什么不能「一个分身走天下」（直接用词向量 X 自己和自己算相关度）？因为「我找什么」和「我是什么」往往不同：「它」这个词作为查询时想找「名词指代对象」，但它作为键时提供的是「代词」这个标签——**同一个词在不同角色下的信息需求完全不同**。分成三个空间后，模型可以独立学习「怎么问」「怎么答」「装什么」，自由度大得多。' },
        { t: 'example', title: '算一遍：带 Q/K/V 的完整注意力', steps: [
          '句子「小猫 坐 垫子」，只看「坐」这个词的注意力。1 维化：「坐」的查询 q = 1；三个词的键：小猫 k = 0.5、坐 k = 0、垫子 k = 2；三个词的值：v猫 = 3、v坐 = 1、v垫 = 8。',
          '**打分**：坐→猫 = 1×0.5 = 0.5；坐→坐 = 0；坐→垫 = 1×2 = 2。',
          '**缩放 + softmax**（这里 d=1 免缩放）：e^0.5 ≈ 1.65、e⁰ = 1、e² ≈ 7.39；总和 10.04。权重：猫 **0.16**、坐 **0.10**、垫 **0.74**。',
          '**加权吸收**：输出 = 0.16×3 + 0.10×1 + 0.74×8 = 0.48 + 0.10 + 5.92 = **6.5**。',
          '解读：「坐」的新表示里 74% 来自「垫子」——模型发现了「坐」和「垫子」（地点）的强关联。这正是语言理解的微缩版：动词和它的论元建立了连接。',
        ], result: '对照公式 Attention(Q,K,V) = softmax(QKᵀ/√d)·V，每一个符号你都亲手代入过数字了。下一课我们把这粒沙放进大楼里。' },
        { t: 'demo', id: 'qkv', title: '六步手算一遍注意力（每步都是真实数字）' },
        { t: 'p', md: '在演示的第 ⑤ 步，你可以直接拖动「坐→猫」「坐→垫子」的原始分数，看 softmax 权重实时变化——**softmax 是个「软选择器」**：分数一高权重迅速聚拢，一低就被边缘化，但**永远不会完全归零**。这个「软」字值千金：硬选择（只看分数最高那一个，其余全忽略）不可导，梯度传不回去，没法训练；softmax 处处可导，误差能穿过注意力流回 Q/K/V 的参数——整个机制才能被端到端训练出来。**深度学习里大量「软」设计（softmax、软注意力、软提示）都源于同一个理由：可导性。**' },
        { t: 'callout', kind: 'tip', title: 'KV Cache 预告', md: '注意一个细节：生成文本时，每来一个新词，只有它的 Q 是新的，**历史所有词的 K 和 V 根本没变**（它们的内容又不会被新词改写——因果掩码保证了单向）——所以可以缓存起来复用，不用每次重算（KV Cache，模块 7.3 详解）。理解了 Q/K/V 的分工，这个优化就顺理成章：**缓存的就是「标签和内容」，因为它们是历史，历史不会变。**' },
        { t: 'quiz', questions: [
          { q: 'Q、K、V 三者的分工是？', opts: ['Q 乘 K 加 V', '查询用于比对、键用于被比对、值是被汇总的信息本体', 'Q 是输入、K 是输出、V 是备份', '三者完全相同'], a: 1, why: 'Q（我要找什么）与 K（我是什么标签）算匹配度，匹配度决定从 V（实际内容）里吸收多少——三个角色缺一不可。' },
          { q: 'W_Q、W_K、W_V 这三个矩阵是？', opts: ['手工设定的常量', '可学习参数——训练让模型学会「如何提问/贴标签/装内容」', '由词频统计得到', '随机生成且不变'], a: 1, why: '它们随梯度下降一起训练。同一句话在不同训练程度下，注意力模式会截然不同。' },
          { q: 'softmax 权重「永不归零、处处可导」对训练意味着？', opts: ['没有意义', '梯度总能流过所有位置，整个操作可以反向传播端到端训练', '注意力是离散的', '必须用强化学习'], a: 1, why: '可导性是反向传播的前提；硬性 argmax（只选最高分）不可导，softmax 的「软」正是为了可学。' },
        ] },
        { t: 'summary', items: [
          '**Q 查询、K 键、V 值**：图书馆检索的三件套，由三个可学习矩阵从 X 变换而来；「找什么」和「是什么」必须分开',
          '匹配度 = Q·K 点积；缩放 ÷√d；softmax 变权重；加权汇总 V（你已带数字算完一遍）',
          'softmax 是可导的「软选择器」，使整个注意力端到端可训练',
          '历史词的 K/V 不随生成变化 → KV Cache 优化的理论根源',
        ] },
      ],
    },
    // ============ 5.3 ============
    {
      id: 'm5l3', num: '5.3', title: 'Transformer 架构逐层拆解', minutes: 20, demoCount: 1,
      emoji: '🏗️',
      brief: '把整栋大楼走一遍：分词、嵌入、注意力+FFN 堆叠、输出层。',
      blocks: [
        { t: 'p', md: '现在把零件组装成大楼。下面这张可点击的架构图就是 GPT 的全部——请先把每个部件都点一遍，读右侧讲解，再回来看本课的梳理：' },
        { t: 'demo', id: 'transformerMap', title: 'Transformer 架构导览（GPT / BERT / 原始版可切换）' },
        { t: 'h', text: '一条 token 的旅程' },
        { t: 'steps', items: [
          { title: '分词与嵌入', md: '「今天天气」→ 4 个 token 编号 → 各查表变成向量（4.2 课），并注入位置信息（5.4 课）。从此网络只处理向量序列——长度 4、每维上万的大矩阵。' },
          { title: '第 1 层：掩码多头自注意力', md: '每个 token 环顾**左边的** token 吸收上下文。为什么只许看左边？因为 GPT 的训练任务是「预测下一个词」——如果偷看了答案，训练就成作弊了。这个限制叫**因果掩码（causal mask）**：把「看未来」的注意力分数强行设成 −∞，softmax 后权重为 0。你在 5.1 演示里打开「GPT 模式」看到的那片灰色三角区就是它。' },
          { title: '残差 + LayerNorm', md: '输出 = 注意力(x) + x 再归一化——3.3 课的梯度直通车 + 3.5 课的稳定工作台，两员老将在此会师。**没有这两件，GPT 堆到第 10 层左右就会训练崩溃。**' },
          { title: '前馈网络 FFN', md: '每个位置独立过两层 MLP：升维 4 倍 → GELU → 降回原维。注意力负责「从别处取信息」，FFN 负责对取来的信息**独自消化加工**。有个著名的研究发现：模型的**事实知识大多存在 FFN 里**（把它看成几万个「键值对记忆」：某个模式触发 → 输出某条知识；研究者甚至成功定位并修改过其中某条记忆，比如把「埃菲尔铁塔在罗马」改回「在巴黎」）。' },
          { title: '× N 层重复', md: '「注意力 + FFN」是一层，GPT-3 堆了 96 层。各层分工在训练中自发形成：低层学语法（词性、搭配）、中层学语义（指代、实体关系）、高层浮现推理与世界知识的运用。' },
          { title: '输出层', md: '取最后一个位置的向量（它经因果注意力吸收了全部上文），乘上「词表大小的矩阵」给每个 token 打分，softmax 变成概率分布：「下一个 token 是什么」的完整预测。回忆 2.6 课：这就是一次 10 类分类的超放大版——类别从 10 变成约 12.8 万。' },
        ] },
        { t: 'callout', kind: 'analogy', title: '打个比方：公司层级', md: '注意力层像**跨部门会议**（横向：各位置互通信息），FFN 像**每个员工的独立思考**（纵向：各自消化加工），残差像**随时可以越级汇报的直通线**，LayerNorm 像**每次会议后的纪律整顿**（拉回稳定分布）。96 层 = 96 级这样的组织，层层抽象，最后输出判断。' },
        { t: 'h', text: '参数都花在哪了：算一笔账' },
        { t: 'example', title: '算一遍：1750 亿参数怎么凑出来的', steps: [
          'GPT-3 的关键尺寸：向量维度 d = 12288，层数 N = 96。',
          '**每层注意力**：Q/K/V/输出投影 4 个 d×d 矩阵 = 4d² = 4 × 12288² ≈ **6.0 亿**参数。',
          '**每层 FFN**：升维矩阵 d×4d 和降维 4d×d = 8d² ≈ **12.1 亿**参数（正好是注意力的 2 倍）。',
          '**每层合计** ≈ 18.1 亿。乘 96 层 ≈ **1740 亿**。',
          '加上词表嵌入层（12.8 万 token × 12288 维 ≈ 16 亿，输入输出共享）——总数正好在 **1750 亿** 附近。账对上了。',
        ], result: '以后看到「7B / 70B / 405B」的模型，你能立刻心算它的规模来源：**每层 ≈ 12d²，总参数 ≈ 12 × d² × 层数**。参数量直接决定两件事：显存占用和推理成本（模块七细算）。' },
        { t: 'callout', kind: 'qa', title: '自问自答', md: '**「为什么 FFN 要先升维 4 倍再降回来？」**——升维是在更宽的空间里展开特征，让「知识键值对」有更多存储位（类比：仓库先用货架摊开再归档）。4 倍是工程经验值，近期研究在尝试 2.5～3 倍省参数。**「GPT 和 BERT 的架构差多少？」**——只差一个开关：因果掩码开 = GPT（只看左边），关 = BERT（左右都看）。原始 Transformer 论文则是编码器（无掩码）+ 解码器（有掩码）两栋楼并用，专为翻译设计。' },
        { t: 'quiz', questions: [
          { q: 'Transformer 一个标准层的组成是？', opts: ['卷积 + 池化', '（多头自注意力 → 残差+LayerNorm → FFN → 残差+LayerNorm）', 'RNN + 注意力', '只有注意力'], a: 1, why: '注意力负责位置间信息交换，FFN 负责逐位置加工，两段都套残差与归一化——这个「三明治」堆 N 层。' },
          { q: 'FFN（前馈网络）在 Transformer 中的角色是？', opts: ['负责并行计算', '对每个位置独立做非线性变换，约占总参数 2/3，被认为是知识存储库', '负责分词', '负责输出概率'], a: 1, why: '注意力横向交换信息，FFN 纵向加工；研究者发现事实性知识大量存储于 FFN 的权重中。' },
          { q: 'GPT 输出「下一个 token」用到了序列哪个位置的向量？', opts: ['第一个位置', '最后一个位置（其向量已汇总全部上文）', '所有位置取平均', '随机位置'], a: 1, why: '因果注意力让最后一个位置吸收了全部前文信息，接输出矩阵 + softmax 即得词表分布。' },
        ] },
        { t: 'summary', items: [
          'GPT 流水线：**分词 → 嵌入+位置 → [掩码注意力 → 残差Norm → FFN → 残差Norm] ×N → 词表打分**',
          '注意力 = 跨位置沟通；FFN = 逐位置加工（知识库，参数占 2/3）；残差 = 信息直通车',
          '规模账：**总参数 ≈ 12 × d² × 层数**——175B 的账你已亲手对上',
          'BERT 与 GPT 只差「因果掩码」开关；原始 Transformer 多了编码器-解码器',
        ] },
      ],
    },
    // ============ 5.4 ============
    {
      id: 'm5l4', num: '5.4', title: '位置编码与并行训练', minutes: 16, demoCount: 0,
      emoji: '🧭',
      brief: '注意力是「睁眼瞎」：不认顺序。位置怎么补进去？为什么这换来百倍速度？',
      blocks: [
        { t: 'h', text: '注意力天生不认顺序' },
        { t: 'p', md: '做个实验：注意力对输入序列**打乱顺序完全无感**——「猫追狗」和「狗追猫」如果用同一组词向量，算出的注意力结果一模一样（打分只看词义，不看位置；输出只是跟着换了顺序）。这对「理解句子」是灾难：语义一半在顺序里。RNN 天生按顺序读所以没这个问题；注意力为了并行把这个「天赋」弄丢了，必须**手动把位置信息补回来**——这就是位置编码（positional encoding）。具体做法朴素到意外：**给每个位置造一个独特的「位置向量」，直接加到词向量上**——词义和位置在同一份向量里共同表达。' },
        { t: 'example', title: '算一遍：位置「指纹」的直觉', steps: [
          '想象用 4 位二进制给位置编号：位置 0 = 0000，位置 1 = 0001，位置 2 = 0010……每个位置的编码独一无二。',
          '正弦编码用的是同样的思路，只是换成**连续版本**：d 维向量里，前几维用高频正弦（变化快，区分相邻位置），后几维用低频正弦（变化慢，编码大范围位置）——像「秒针 + 分针 + 时针」的组合报时，任何时刻的组合都独一无二。',
          '两个位置越接近，它们的编码向量越相似（点积越大）——**「距离」被编码进了向量几何**，注意力可以轻松学会「只关注离我近 2 格的词」这类模式。',
        ], result: '词向量（词义）+ 位置向量（座位号）相加后，「猫追狗」和「狗追猫」的输入不再相同——注意力有了顺序感。' },
        { t: 'callout', kind: 'analogy', title: '打个比方', md: '注意力像**只看证件照不看桌签的会议记录员**：谁能发言、说了什么记得清清楚楚，但谁先谁后一概不知。位置编码 = 给每个人发一张**带座位号的桌签**（向量加到词向量上），顺序信息回来了。' },
        { t: 'h', text: '三代位置编码' },
        { t: 'list', items: [
          '**正弦位置编码**（2017 原版）：用不同频率的正弦/余弦波给每个位置生成独特「指纹」，加到词向量上。优点：公式固定、能外推到没见过的长度（第 10001 个位置也有指纹）。',
          '**可学习位置编码**（BERT/GPT-2）：每个位置一个可训练向量（最多训 512 个位置就只有 512 张桌签），简单有效，但**最长只认训练时见过的位置数**，超长就抓瞎。',
          '**RoPE 旋转位置编码**（现代主流，LLaMA/Qwen/DeepSeek 等）：更巧妙的思路——不「加」位置向量，而是把 q/k 向量按位置**旋转**相应角度（第 p 个词转 p×θ）。妙处：两个词的注意力分数只取决于它们的**相对距离**（转角之差），「隔 3 个词」在句首和句中间是同一种几何关系。性质优雅、外推性好，配合 NTK/YaRN 等插值技巧能把上下文窗口扩展到百万级 token（7B 模型从 4K 拉到 128K+ 就是这么干的）。',
        ] },
        { t: 'h', text: '并行的代价与收益' },
        { t: 'p', md: '补上位置编码后，Transformer 成了「完全并行」的架构：**训练时整句话的所有位置同时算**——把整句的 Q、K、V 各拼成一个大矩阵，一次矩阵乘法出全部注意力分数（GPU 上万核心火力全开）；RNN 则必须一步一步串行，第 500 个词必须等第 499 个算完。算一笔账：训练数据动辄包含百万词的长文档批次，RNN 要串行跑百万步，Transformer 只需要几十次大矩阵乘——**同样的算力预算，训练效率高出一到两个数量级**。这是它能「大力出奇迹」的物理基础：不是Transformer 的单步更聪明，而是它能吃满硬件。' },
        { t: 'callout', kind: 'warn', title: '但推理时只能并行「读」，不能并行「写」', md: '训练时所有答案（文本本身）都是已知的，可以并行「看」；可**生成**时每个新词都依赖前面刚生成的词——这是本质上的串行（自回归）。这就是为什么模型读 100 页文档很快（一次并行处理）、写字却一个字一个字往外蹦（每字一次前向）。KV Cache 就是为这个阶段提速的（7.3 课见）。' },
        { t: 'quiz', questions: [
          { q: '为什么必须加位置编码？', opts: ['增加参数量', '注意力计算与顺序无关（置换等变），需要显式注入位置信息', '为了让词向量更长', '防止过拟合'], a: 1, why: '打乱输入顺序，注意力输出只是相应换序——顺序语义必须靠位置编码补充。' },
          { q: 'RoPE（旋转位置编码）的优雅之处是？', opts: ['参数最少', '以「旋转」实现，注意力分数只依赖相对位置，外推性好', '不需要训练', '能自动分词'], a: 1, why: '相对位置性质让模型天然关注「隔多远」而非「在第几位」，配合扩展技巧支撑了现代长上下文。' },
          { q: 'Transformer 训练能全并行、推理生成却只能逐 token，原因是？', opts: ['GPU 不够', '训练时目标文本已知可同时「看」；生成时每个新词依赖前面刚生成的词，本质串行', '位置编码限制', 'softmax 太慢'], a: 1, why: '训练是「老师已给全文」的对答案任务；推理是自回归的接龙，天然串行——这正是 KV Cache、批处理等推理优化存在的意义。' },
        ] },
        { t: 'summary', items: [
          '注意力**置换等变**：不认顺序 → 必须加位置编码（词向量 + 座位号）',
          '三代方案：正弦指纹 → 可学习桌签 → **RoPE 旋转**（相对位置，现代主流，支撑百万级上下文）',
          '位置编码换来的是**训练全并行**——大模型「大力出奇迹」的物理基础',
          '生成仍本质串行 → 引出推理优化（KV Cache）的伏笔',
        ] },
      ],
    },
    // ============ 5.5 ============
    {
      id: 'm5l5', num: '5.5', title: 'GPT 如何生成文本', minutes: 20, demoCount: 1,
      emoji: '🀄',
      brief: '自回归接龙 + 温度/top-p 采样——ChatGPT 打字机的真相。',
      blocks: [
        { t: 'h', text: '预训练的本质：亿亿次的「文字接龙」' },
        { t: 'p', md: 'GPT 的预训练目标朴素得令人意外：**给定前面的词，预测下一个词**。把互联网文本切成长序列，让模型对**每个位置**都做一次「猜下一个」（一句话 100 个词 = 100 道免费的题），猜错就按反向传播更新参数。没有人工标注——文本本身就是无限大的题库（**自监督学习**，4.1 课埋的伏笔在这兑现）。数万亿 token 训练下来，「接龙」能力里涌现出了语法、事实知识、翻译、甚至推理的雏形。' },
        { t: 'callout', kind: 'analogy', title: '为什么接龙能通向智能', md: '想接好「地球绕着__转」就得懂天文；想接好「她把文件放进包里，然后拉上了__」就得理解物理状态（拉上的是包，不是文件）；想接好一段数学推导就得会推理。**预测下一词是最廉价却最丰富的学习信号**——它强迫模型压缩整个文本世界的规律。有一句名言概括这种信念：「要成为一个好的文本预测器，你必须理解产生文本的世界本身。」' },
        { t: 'h', text: '生成：自回归循环' },
        { t: 'p', md: '训练完成后，生成就是一个循环：输入 prompt → 模型输出词表分布 → 按某种策略抽一个 token → 拼回输入尾部 → 再进模型 → ……直到输出结束符。**每个字都见过前面所有的字**（包括自己刚写的），所以长文能保持连贯；这也是为什么它写作文从不「先打草稿再誊写」——它没法回头改，错了只能将错就错往下圆。下面亲手玩一遍这个循环——这次是你在浏览器里现场训练出来的小模型：' },
        { t: 'demo', id: 'sampling', title: '现场训练小模型 + 逐字生成 + 温度/top-p 实验室' },
        { t: 'h', text: '两个采样旋钮' },
        { t: 'formula', tex: `p_i^{\\text{new}} = \\frac{\\textcolor{${C.blue}}{p_i^{1/\\textcolor{${C.red}}{T}}}}{\\sum_j p_j^{1/\\textcolor{${C.red}}{T}}} \\qquad \\text{top-}p：\\text{只保留累计概率} \\ge \\textcolor{${C.violet}}{p} \\text{的最小候选集}`,
          terms: [
            { color: C.red, frag: 'T', name: '温度 Temperature', desc: '<1 尖锐保守（几乎总选最高分），>1 平坦冒险，0 ≈ 永远选第一名（贪心）' },
            { color: C.violet, frag: 'p', name: 'top-p（核采样）', desc: '只在「累计概率前 p」的候选里采样，砍掉长尾的胡言乱语' },
          ],
          note: '温度作用于「分布形状」，top-p 作用于「候选名单」——两者常配合使用。写代码求稳用低温，写诗找灵感调高温。' },
        { t: 'example', title: '算一遍：温度如何改变命运', steps: [
          '模型对下一个词的原始预测：「猫」0.6、「狗」0.3、「鱼」0.1。',
          '**低温 T = 0.5**：每个概率取平方（1/T = 2）→ 0.36、0.09、0.01，重新归一化（除以总和 0.46）→ **0.78、0.20、0.02**。第一名从 60% 涨到 78%——分布变尖锐，输出更稳、更可预测。',
          '**高温 T = 2**：每个概率开方 → 0.775、0.548、0.316，归一化 → **0.47、0.33、0.20**。三者的差距被抹平——冷门词「鱼」从 10% 冒到 20%，输出更多样、更冒险。',
          '**再叠加 top-p = 0.9**：低温下累计 0.78+0.20 = 0.98 ≥ 0.9 → 候选 {猫, 狗}，「鱼」被除名；高温下 0.47+0.33 = 0.80 < 0.9 → 三个词都保留。',
        ], result: '**温度改形状，top-p 定名单**——合起来决定「下一个词」花落谁家。API 里常见的 temperature=0.7、top_p=0.9 之类的参数，你现在完全知道它们在干什么了。' },
        { t: 'callout', kind: 'qa', title: '自问自答：三个常见现象', md: '**① 为什么模型每次回答不完全一样？**——采样带随机性（哪怕同一个问题），温度 > 0 时每次抽中的词不同，而文字是逐词的，一步分岔后面全变。**② 为什么有时它「卡壳复读」？**——低温下分布尖锐，总选同一个词；或模型对某段文字预测置信度极高，就会绕圈复读。**③ 为什么会「一本正经胡说八道」（幻觉）？**——它只是在选「接得上」的词，**没有事实核查机制**：编一个不存在的论文引用，语法流畅、格式完美，接龙意义上毫无破绽。连贯 ≠ 正确。对齐训练（模块六）能缓解但不能根除——这个你现在能从原理上理解它了。' },
        { t: 'quiz', questions: [
          { q: 'GPT 预训练的学习信号来自？', opts: ['人工标注问答', '文本自身：每个位置「预测下一个 token」，猜错即更新（自监督）', '强化学习奖励', '用户点赞'], a: 1, why: '互联网文本既是输入也是题库，无需人工标注——这让训练数据可以无限扩展。' },
          { q: '温度 T 调到接近 0 会发生什么？', opts: ['输出完全随机', '分布极度尖锐，几乎总选概率最高的 token（贪心，输出固定但可能复读）', '模型变笨但更快', '词表缩小'], a: 1, why: 'T→0 时 softmax 退化为 argmax：每次都选第一名，输出确定性最高，也最容易陷入复读。' },
          { q: '「自回归生成」指的是？', opts: ['一次生成整段', '逐 token 生成，且把新 token 拼回输入继续生成下一步', '回归到数据集重新训练', '用回归分析拟合文本'], a: 1, why: '自己生成的输出成为自己下一步的输入——这个循环就是「自回归」，也是打字机效果的来源。' },
        ] },
        { t: 'summary', items: [
          '预训练 = **在海量文本上做下一词预测**（自监督），接龙能力中涌现知识',
          '生成 = **自回归循环**：预测 → 采样 → 拼回 → 再预测；无法回头修改，只能将错就错',
          '**温度**改分布形状（0.6→0.78 你已算过），**top-p** 定候选名单',
          '幻觉的根源：模型追求「接得上」而非「是真的」——为模块六的对齐埋伏笔',
        ] },
      ],
    },
    // ============ 5.6 ============
    {
      id: 'm5l6', num: '5.6', title: 'BERT vs GPT：两大范式', minutes: 16, demoCount: 0,
      emoji: '⚖️',
      brief: '同一棵树上的两条路：理解（双向）与生成（单向），谁笑到了最后？',
      blocks: [
        { t: 'h', text: '一个架构，两种「修炼方式」' },
        { t: 'p', md: 'Transformer 架构本身是中性的——注意力的「看不看得见未来」由因果掩码开关决定。同一个开关，演化出两条预训练路线，各自修炼出不同的绝活：' },
        { t: 'table', head: ['维度', 'BERT（2018）', 'GPT（2018-）'], rows: [
          ['结构', '只用编码器（双向注意力）', '只用解码器（因果掩码，单向）'],
          ['预训练任务', '完形填空：遮住 15% 的词猜原词 + 下一句判断', '下一词预测（接龙）'],
          ['擅长', '理解：分类、问答、检索、匹配', '生成：写作、对话、代码、推理'],
          ['使用方式', '训练后接任务头微调，一次一个任务', '同一个模型，靠提示词切换任意任务'],
          ['现状', '在检索/ embedding 模型中仍有生命力', '**大模型绝对主流**'],
        ] },
        { t: 'callout', kind: 'analogy', title: '填空题 vs 接龙题', md: 'BERT 像**做完形填空的学霸**：左右全文都看得到（「银行」旁边有「存钱」还是「河流」，一看便知该取哪个义项），理解深、判断准，但只会「补空」不会「创作」。GPT 像**写小说的连载作者**：只能看前文，练出强大的续写能力——而关键洞察在于：**「问答」「翻译」「摘要」全都可以写成续写的形式**。' },
        { t: 'example', title: '看一遍：同一个任务在两种范式下的样子', steps: [
          '**任务：把「I love cats」翻译成中文。**',
          '**BERT 的做法**：先预训练（学会理解），再针对「翻译」任务**单独微调**——把模型顶部接一个翻译专用的输出层，拿几十万条平行句训练这个新头。做好翻译后，如果明天要做摘要？**再接一个摘要头、再微调一轮**。一任务一头，各自为政。',
          '**GPT 的做法**：什么都不加、什么都不训，直接给一段文字：「请把下面的英文翻译成中文：I love cats。中文：」——**模型续写出「我爱猫」**。明天要摘要？换个提示词就行，模型同一个。',
          '**关键差别**：BERT 的「理解」能力要靠**每个任务单独接线**才能用出来；GPT 的能力靠**提示词召唤**——因为它的预训练任务（续写）和使用方式（续写）天生同构。',
        ], result: '这就是「提示学习（prompting）」的起源：**任务 = 一段文字描述 + 续写**。2020 年 GPT-3 论文把这个能力命名为「上下文学习（in-context learning）」——不用改一个参数，几句示例就能教会它新任务，这在 BERT 范式里是不可想象的。' },
        { t: 'h', text: 'GPT 范式胜出的三个理由' },
        { t: 'list', items: [
          '**训练与生成同构**：预训练任务（接龙）和使用方式（生成）完全一致，预训练学到的东西零损耗地用在推理上，无需额外的任务头设计。',
          '**一句话调用一切任务**：「请把下面英文翻译成中文：…」就是一道续写题——**提示学习让一个模型服务无限任务**，这是 BERT「一任务一微调」模式做不到的。',
          '**Scaling 的顺风车**：下一词预测天然吃海量无标注数据 + 完全并行训练，模型越大能力越强（Scaling Law，模块六）——BERT 的完形填空也有 scaling，但「补空」能力的商业天花板远低于「生成」。',
        ] },
        { t: 'callout', kind: 'qa', title: '自问自答：BERT 消失了吗？', md: '没有，它**转行了**。今天的搜索引擎、RAG 系统里的「语义检索」，大量使用 BERT 系的双向编码模型：把问题和文档各自压成一个向量（4.2 课的语义城市），按距离找最相关的——这活儿不需要生成能力，双向理解反而更合适、还便宜。所以格局是：**理解类、检索类任务用 BERT 系编码器；生成类、通用任务用 GPT 系解码器**。你会在 7.1 课的 RAG 里再见到它。' },
        { t: 'callout', kind: 'story', title: '从 GPT-1 到 ChatGPT 的五级跳', md: 'GPT-1（2018，1.17 亿参数）验证思路；GPT-2（2019，15 亿）能写长文，当时被认为「太危险不许完全公开」（现在看有点好笑，但「AI 生成文本滥用」的担忧确实始于彼时）；GPT-3（2020，1750 亿）展现少样本学习能力， Scaling Law 声名大噪；InstructGPT/ChatGPT（2022）加上指令微调与 RLHF，从「补全机」变成「助手」，引爆全球；GPT-4（2023）多模态化。**这条路线 = 模块六的全部内容：预训练 → 指令微调 → 对齐**。下一模块见。' },
        { t: 'quiz', questions: [
          { q: 'BERT 与 GPT 的本质区别是？', opts: ['BERT 更大', 'BERT 双向注意力做理解（完形填空），GPT 单向因果做生成（接龙）', 'BERT 不用注意力', 'GPT 不用预训练'], a: 1, why: '同一个架构家族，掩码开关决定双向/单向，进而决定预训练任务与擅长方向。' },
          { q: '「一个 GPT 模型靠提示词就能做翻译/摘要/问答」，这依赖的机制是？', opts: ['多任务头', '预训练任务与使用方式同构：任何任务都能写成「续写」形式（提示学习）', '模型内置翻译器', '知识图谱'], a: 1, why: '生成式范式把任务统一成「给上文续下文」，一个模型 + 不同 prompt 即可切换任务。' },
          { q: 'ChatGPT 相比 GPT-3 的关键飞跃是？', opts: ['参数量翻倍', '加上指令微调与 RLHF 对齐，从「会续写」变成「按指令办事的助手」', '换了编程语言', '上下文变短'], a: 1, why: '基座能力（GPT-3）之上，指令微调教会「听指令」，RLHF 教会「符合人类偏好」——这正是下一模块的主题。' },
        ] },
        { t: 'summary', items: [
          'BERT = 双向理解（完形填空）；GPT = 单向生成（接龙）',
          'GPT 范式胜出：**任务可统一为续写 → 提示学习一模型多用 → 吃满 Scaling**',
          'BERT 没消失：转行做了检索/embedding（RAG 里你会再见到它）',
          'ChatGPT = GPT-3 基座 + 指令微调 + RLHF——「预训练 → 微调 → 对齐」三部曲，下一模块展开',
        ] },
      ],
    },
  ],
}
